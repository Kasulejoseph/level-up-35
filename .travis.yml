language: python

python:
    - "3.6"

# install dependency
install: 
  - pip install -r requirement.txt
  - pip install coveralls

script:
  - coverage run -m unittest Tests/test_register.py -v
  - 
after_success: 
- coverage report -m
- coveralls
